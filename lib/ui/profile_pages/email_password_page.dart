import 'package:devup/model/user.dart';
import 'package:flutter/material.dart';

class EmailPasswordPage extends StatelessWidget {
  final User tempUser;

  const EmailPasswordPage({Key key, @required this.tempUser}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}